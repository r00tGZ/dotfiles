# Kali Installer (amd64), VirtualBox: one SATA disk, NAT/DHCP, Internet access.
# WARNING: automatically erases /dev/sda.
# Start through Advanced options -> Automated install, then load this file.

# English system, Spanish keyboard, Spain.
d-i debian-installer/language string en
d-i debian-installer/country string ES
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select es

# Networking.
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string kali
d-i netcfg/get_domain string
d-i netcfg/hostname string kali

# User account; root login disabled, administration through sudo.
d-i passwd/root-login boolean false
d-i passwd/user-fullname string Kali
d-i passwd/username string kali
d-i passwd/user-password password s3cr3t
d-i passwd/user-password-again password s3cr3t

# Clock.
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Madrid
d-i clock-setup/ntp boolean true

# Entire disk; ordinary partitions, one root filesystem plus swap.
d-i partman-auto/disk string /dev/sda
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic

# Permit removal of old LVM/RAID metadata during reinstalls.
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-md/confirm boolean true

# Confirm partitioning automatically.
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Kali mirror; no proxy or additional installation media.
d-i mirror/protocol string http
d-i mirror/country string manual
d-i mirror/http/hostname string http.kali.org
d-i mirror/http/directory string /kali
d-i mirror/http/proxy string
d-i apt-setup/use_mirror boolean true
d-i apt-setup/cdrom/set-first boolean false
d-i apt-setup/disable-cdrom-entries boolean true
d-i apt-setup/services-select multiselect

# Xfce, default Kali tools, requested utilities and VirtualBox guest tools.
tasksel tasksel/first multiselect standard, core, desktop-xfce, meta-default
d-i pkgsel/include string curl git sudo tmux vim zsh virtualbox-guest-x11

# Install GRUB and reboot without the final confirmation.
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string /dev/sda
d-i finish-install/reboot_in_progress note
